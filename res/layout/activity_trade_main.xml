<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".TradeMainActivity" >

    <ImageButton
        android:id="@+id/trade_main_home_btn"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_alignParentRight="true"
        android:layout_alignParentTop="true"
        android:onClick="onHomeBtnClicker"
        android:src="@drawable/home" />

    <LinearLayout
        android:id="@+id/trade_main_bottom"
        android:layout_width="match_parent"
        android:layout_height="75dp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true" >

        <Button
            android:id="@+id/trade_main_search"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:drawableTop="@drawable/search"
            android:text="筛选"
            android:textSize="13sp" />

        <Button
            android:id="@+id/trade_main_mine"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:drawableTop="@drawable/trade_box"
            android:text="我的奖品"
            android:textSize="13sp" />

        <Button
            android:id="@+id/trade_main_person_centre"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:drawableTop="@drawable/trade_user"
            android:text="个人中心"
            android:textSize="13sp" />

        <Button
            android:id="@+id/trade_main_more"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:text="更多..."
            android:textSize="13sp" />
    </LinearLayout>

    <ListView
        android:id="@+id/trade_main_list"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@+id/trade_main_bottom"
        android:layout_below="@+id/trade_main_home_btn"
        android:layout_centerHorizontal="true"
        tools:listitem="@layout/trade_main_list_item" >

    </ListView>

</RelativeLayout>